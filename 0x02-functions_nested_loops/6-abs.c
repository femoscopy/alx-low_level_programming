#include "main.h"
/**
 * _abs - printsints theabsolute function of the number
 * @n: the number to be evaluated
 * Return: the absolute value of the number
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
